{% extends 'base.html.twig' %}

{% block title %}{{ 'page_title.client.new.main'|trans }}{% endblock %}

{% block main_content %}
    <div class="container-fluid">
        <div class="d-sm-flex align-items-center justify-content-between mb-4">
            <h1 class="h3 mb-0 text-gray-800">{{ 'page_title.client.new.second'|trans }}</h1>
            <a href="{{ path('app_client_list') }}" class="btn btn-secondary">Retour</a>
        </div>
        {% include 'client/_form.html.twig' %}
    </div>
{% endblock %}
